#include <stdlib.h>
/**
 * _calloc- wtv
 * @nmemb: n
 * @size: n
 *
 * Return: p
 */
void *_calloc(unsigned int nmemb, unsigned int size)
{
	void *ret;

	ret = malloc(b);
	if (ret == NULL)
		exit(98);

	return (ret);
}
