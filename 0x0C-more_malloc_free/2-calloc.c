#include <stdlib.h>
/**
 * malloc_checked- wtv
 * @b: n
 *
 * Return: p
 */
void *_calloc(unsigned int nmemb, unsigned int size)
{
	void *ret;

	ret = malloc(b);
	if (ret == NULL)
		exit(98);

	return (ret);
}
